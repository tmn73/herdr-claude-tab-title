{
  "name": "herdr-agent-title",
  "version": "0.1.0",
  "description": "Mirrors each coding agent's own session title onto its Herdr tab",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.1.34"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
