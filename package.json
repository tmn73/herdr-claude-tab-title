{
  "name": "herdr-claude-tab-title",
  "version": "0.3.0",
  "description": "Mirrors each Claude Code session title, and its agent state, onto its Herdr tab",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.1.34"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
